# 23 [M1-6] 回录与 Settlement（纸面闭环）

Type: impl
Status: resolved
Blocked by: 33, 36, 37

## 范围

投注记录闭环：Bet/BetLeg/BetSlip 落地、注级建议清单（含未购标记）+ 票级回录（勾选实际购买子集，票 06 决定）、paper 模式；DrawResult 导入与 Settlement 引擎——按官方规则结算（无效场次：单关退款/串关腿按 1/不足 2 关整单退款；任9 复式按 Combination 逐个判定）；bankroll 只受 live 影响；复盘列表（结算状态/盈亏）。

## 验收

- 一个真实轮次的纸面全流程跑通：建议 → 回录 → 官方开奖导入 → 结算 → 复盘列表。
- 无效场次结算规则有单测覆盖（含腰斩/延期场景）。

## Answer

2026-09-13 实施完成。`settlement.py` 纯函数引擎（官方规则：单关退款/
串关腿按 1/去除后不足 2 关整单退款/任9 复式逐组合判定，hhad 半球线
不取整比较，hafu 缺半场比分→待开）+ 服务层 `run_settlement`（live 注
联动 bankroll 事件）+ API（建注/票级回录勾选子集/开奖导入/结算批跑/复盘列表）。
Web 投注页全流程可用。无效场次（腰斩/延期）场景有单测：
test_settlement_with_void_fixture、test_single_void_refunds、
test_parlay_* 系列。「一个真实轮次的纸面全流程」已用真实竞彩数据
（39 场）+ 手工开奖导入在本地跑通（今日页→建注→回录→导入→结算→列表）。

## 当前验收修订（2026-09-13）

API/服务层已有实现；原“Web全流程可用”证据不足，且存在无效腿/资金/更正错误。

原答案保留为历史，不代表当前通过。依赖票完成后，本票只核对剩余验收并链接证据，不重复开发；不满足仍保持未验收。当前计划见[Spec v1.1](../spec.md)，后继工作见[可信纸面闭环验收](36-trusted-paper-user-loop.md)。

票33已修复结算/资金/更正错误并通过本地全量检查；本票仍等待36/37的浏览器业务与真实运行证据，不以服务测试替代验收。
