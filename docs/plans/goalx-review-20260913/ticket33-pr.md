## 变更类型

- [x] 🐛 fix: Bug 修复
- [x] 📝 docs: 文档变更

## 变更描述

此前2串1一腿无效会因剩余腿不足2而整单退款，回录无重复/状态保护，未购live命中也会入账，开奖覆盖后已结注单不会重算。现在无效腿按赔率1、剩余腿照常计奖；回录原子校验并拒绝重复，只有已购live影响资金；更正需原因、保留旧新事实和结算历史，按兑付差额冲正且重试幂等。

新增v4审计表及只读 `task audit-ledger`，旧异常须人工核查，升级不改写历史资金。未实现的live奖池票拒绝，paper池票保持待结；未购反事实从正式yield、已结数量和CLV排除。README、领域约束和ADR同步收敛到可信纸面闭环，保留技术栈，暂缓完整LLM融合与奖池扩展。

## 影响范围

- [x] `apps/backend` - FastAPI 后端
- [x] `apps/web` - 生成API类型
- [x] `contracts/openapi` - 更正原因、拒绝响应和池票草稿说明
- [x] 其他: README、领域文档与只读运维命令

## Definition of Done

- [x] `task check` 全部通过
- [x] 覆盖率达标（backend 94.17%，web阈值通过）
- [x] basedpyright / tsc 0 错误
- [x] API 变更已同步契约与生成类型
- [x] 命名符合项目约定，无冗余代码

## 测试说明

六个最小业务回归先RED后GREEN；最终179项后端测试、19项前端测试、1项契约一致性及4项Playwright冒烟通过。覆盖双连接并发回录、写入后失败的完整回滚、赢输双向冲正、半场事实撤回/补全、同事实幂等、未购隔离、v3升级保留旧账及只读审计。

## 其他说明

升级需按README先核查并备份，再迁移。页面的更正原因输入、paper/live统计分组、注/腿分母和赛前证据属于后继工作。本次浏览器冒烟不是完整had纸面流程验收，未宣称连续运行或真钱资格；未部署、购买或执行运行库结算。详细实施票按仓库约定保存在本地ignored的`.scratch`，不随PR发布。
