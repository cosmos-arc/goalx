# Goalx 目标、研究、设计与实现审视

审视日期：2026-09-13。代码基线：`8cf231fac3814c59ce61923ecf524f2a191e1c3b`，main。
（注：文中 `文件:行号` 引用按该基线定位；PR #9 领域包重构后 services.py、store/results.py、clv.py、forecast.py、backtest.py、haircut.py、evaluation.py、dc_model.py、ingest/oddsapi.py 等已移入对应领域包，旧链接不随之改写。）

后续状态：用户已要求按五票方案更新计划，现行入口为[Spec v1.1](../goalx-quant/spec.md)。以下保留审视当时的事实与建议，代码修复和真实验收尚待实施。

本报告是分析建议，不替代用户已确认的 spec/ADR，不代表已批准重排路线图。原设计地图已抵达设计目的地；本轮的待决事项另见 [目标对齐与最小下一阶段](map.md)。未修改产品代码或运行库。

## 判断

项目目标清楚，单机技术架构总体适度；主要问题是**功能铺设快于盈利假设验证，工程完成与产品验收混在一起，若干研究推论被提升成了设计前提**。

建议保留 M1/M2 投入，不推倒重来；先把“结果可信、用户能完成一轮纸面记录、能持续取得有效赛前数据”做实，再选择一个主攻方向。完整 LLM 双线、全玩法与覆盖优化不应成为开始验证价值的前置条件。

## 依据与验证范围

- 阅读现有 wayfinder 地图、spec、领域术语表、7 份 ADR、研究和实施票；重点核查盈利假设、双线架构、验证协议、M1/M2 代码。
- 对采集/模型验证和产品资金闭环分别做独立只读核查；外部研究抽查一手来源。
- 当前工作树起始干净。GitHub 查询确认 M2 PR 已合并；[main 对应 CI](https://github.com/cosmos-arc/goalx/actions/runs/34732420988) 成功。此项只证明工程检查结果。
- 本轮通过 `task test-fast` 重跑验证 API、CLV、欧赔采集、投注仓储、结算五组测试：**38 passed**。没有重复全量 gate，没有浏览器真实轮次验收。
- 三个资金/事实生命周期问题用独立内存 SQLite 复现，没有改动业务 DB。
- 以下运行事实来自 `data/goalx.db` 的只读查询，仅代表本机此库；其他临时库中可能存在过去的冒烟数据。

| 运行事实 | 当前值 | 能证明什么 |
|---|---:|---|
| Fixture / 已关联欧赔 | 39 / 26 | 实采与部分关联已存在 |
| 历史比赛 | 10,438 | 模型训练/基线实验有数据 |
| ML Forecast | 16 | 预测确已生成入库 |
| Bet / DrawResult / CLV | 0 / 0 / 0 | 此库尚无纸面闭环积累 |
| closing 目的快照 | 0 | 此库尚无真实收盘窗口证据 |
| PoolPeriod / PublicShare / MatchIntel | 0 / 0 / 0 | 奖池与 LLM 尚无运行数据 |
| 已完成回测 | 1 | 实验管线已跑通 |
| 回测预测 / 注单 | 5,238 / 4,154 | 后者含单关与串关，不是独立样本数 |
| RPS skill | −3.751% | 此样本上 DC 平均评分劣于所用市场基准 |
| 投入加权 ROI | −25.3335% | 合成报价实验的结果，不是实盘收益 |

## 符合目标，应该保留

1. **个人、本地、人工下单边界。** FastAPI + React + SQLite WAL 足够；无需 SaaS、多租户、分布式数据库或通用交易平台。
2. **官方结果入口、赔率/预测不可变、统一 Settlement。** 都直接服务可复盘性；已有 append-only 触发器。内容哈希是完整性和去重工具，不是独立第三方时间证明，个人系统不必因此引入区块链。
3. **paper/live 统一 Bet。** 避免维护两套算法；需要修的是状态转换和统计分组，不是拆成两个系统。
4. **市场基准、时间切分、公开负面结果。** 回测训练窗确实早于比赛周；未把负收益隐藏掉，值得保留。
5. **回测收窄到 had-only。** 1X2 不足以唯一识别完整比分/总进球分布。停止用反推的 hhad/ttg 报价制造虚假优势是合理收缩；应同步 spec/ADR 的当前适用范围。
6. **比分矩阵作为全场比分相关市场的共同表示。** had/hhad/ttg/crs 共用分布合理；不必因为当前只验证 had 就删除成熟纯函数。
7. **Prefect 的薄包装、契约与 CI。** Prefect 是用户已有选型，当前无需换框架；应把已有 flows 的部署与窗口运行做好。API 契约和检查成本尚不是主要瓶颈。

## 目标与路线图：最值得改变的部分

### 盈利假设排在辅助能力之后

[spec](../goalx-quant/spec.md) 将 A=陈盘、B=滚存列为主攻，ML/LLM 为辅助，但路线是 M2 模型→M3 完整 LLM→M4 EV/成本/滚存。这会先建设数周辅助能力，最后才检验它们服务的机会是否存在。

建议下一阶段先做一个可证伪的小闭环：同步赛前报价→候选及拒绝原因→锁定纸面选择→官方结果→收益/CLV→成本。A 方向可优先，因为已有大部分采集组件；这是实现成本上的推荐，不是 A 已被证明更能盈利。B 先做滚存、销售额、开奖奖级与公众份额的可用性抽样，不承诺建设优化器。

结束条件应是“机会是否真实、能否在有效窗口完成记录、数据预算是否承担得起”，不能只是又多几页或多一个模型。短期连续运行只验工程与信号可观察性，不据此宣称长期收益；保留原先整赛季要求，除非用户重新裁决。

### ML 对市场 skill 门槛需按使用场景理解

若直接用 ML 定价，ML 的有效性必须证明；但市场概率驱动的陈盘扫描可以在 DC 尚落后时独立积累纸面证据。把“辅助 DC 必须全市场评分超过市场”设为任何 A 方向研究的前置，会卡住与它逻辑上不同的假设。

建议评估实际采用的策略/概率版本，分别保留纯市场、纯 ML、将来融合的结果。不是降低通过线，也不是用筛选子样本美化成绩。

### 先证明 LLM 的增量价值

暂缓完整 scout/gate/analyst + LEAP 自动改概率。最小探索只需要少量场次的结构化情报、来源、发布时间、矛盾与事后标签；比较它是否比不用 LLM 更早、正确地发现可用信息。

当前流程文字还存在依赖不清：gate 使用“融合后 vs 市场”的分歧决定是否运行 analyst，但 analyst 又产出融合似然项。需要明确初筛分布与复核后分布。JS 0.02/0.06、似然权重、0.45 半场拆分均应是待校准参数，而不是得到实证支持的常量。

多个新闻源可能转载同一消息，市场也可能已经定价；直接相乘似然会重复计入信息。先证明有新增信息，再讨论融合形式。保留 ADR 0004 的 SDK 选择，无需重新做框架竞赛。

## 必须先修的正确性与验收问题

### 1. 无效场次规则从文档传播到代码和测试

[Settlement](../../../apps/backend/src/goalx_backend/settlement.py#L231) 在串关剩余不足两腿时整单退款。官方说明是无效腿按 1 计奖，并不意味着剩余一腿自动退款；另一腿仍应按原赔率及结果处理。[广东体彩官方说明](https://www.gdlottery.cn/html/ticaidongtai/20240108/89644.html)

例如两腿中一腿无效，另一腿输：当前规则退款，会把应亏损的票记成不亏。另一腿赢且赔率为 2：当前只退本金，会少算奖金。应一起更正文档、引擎和测试，不能只修实现后留下错误需求。现有测试通过恰好说明测试固化了同一错误。

### 2. 购买与资金生命周期不一致（已独立复现）

- [未购 live 也兑付](../../../apps/backend/src/goalx_backend/services.py:220)：批跑扫描全部 open 注，兑付仅判断 mode。未购 10 元、赔率 2 的建议命中，Bankroll 增加 20 元。
- [重复回录重复扣款](../../../apps/backend/src/goalx_backend/services.py:141)：同一 10 元注回录两次，创建两票、扣款 20 元；关联也被覆盖。应拒绝重复 ID、已购/已结状态，保障事务与幂等。
- [结果修正与结算脱节](../../../apps/backend/src/goalx_backend/store/results.py:14)：DrawResult 可以覆盖，已结注不重跑；将赢改输后仍保留原兑付。最小修复是更正审计、受影响记录与差额冲正，不需要完整事件溯源平台。

未购建议可以单独计算反事实结果，但不能进入 live 资金或正式购买样本。paper/live 共用算法不等于合并账本。

### 3. 验证页把未知当通过

[验证进度](../../../apps/backend/src/goalx_backend/api/validation.py#L207) 将“复核无记录”设 `achieved=True`；skill 来自最新历史回测；没有整赛季条件。因此三项绿灯的语义不等于既定纸面通过条件。

应显示“未评估”，区分回测与前瞻样本，固定模型/策略版本、起止区间和纳入标准。即使暂时没有任何真钱放行按钮，看板错误也会误导判断。

### 4. CLV 的样本单位、来源与时点不一致

[CLV](../../../apps/backend/src/goalx_backend/clv.py:101) 每腿记一行，验证页把行数叫“注”；100 张2串1可能计成200。paper/live/未购建议不分组，同场重复出现不是独立证据。

[收盘窗口](../../../apps/backend/src/goalx_backend/clv.py:26) 允许开赛后30分钟，可能纳入赛中信息。正常抓取路径筛尚未开球场次，当前库也无 closing 记录，不能声称已经发生污染；但有效性校验必须拒绝赛后报价。

实际使用的是多 book 共识，不是 spec 的 Pinnacle 优先，也尚无历史 PSC 补漏。建议明确一种有质量标签的基准，记录报价时间、抓取时间、缺失率；分列票数、腿数、场次数，按实际策略聚合。beat rate 单独不衡量正负幅度、成本或有效独立样本，保留作诊断而非充分证明。

### 5. Forecast 哈希未替代赛前约束

[日常预测](../../../apps/backend/src/goalx_backend/forecast.py:159) 可对过去业务日加载最新模型生成，没有检查训练窗早于目标比赛，也未拒绝赛后生成。issued_at 保存当前时间是对的；仍需把正式赛前预测和赛后 replay 分开。此处是确定缺失的约束，不是已证实16条预测污染。

### 6. Web 纸面闭环缺第一步

今日页没有选注/建建议入口；投注页只能勾选已存在的建议。[createBet API 包装](../../../apps/web/src/api/goalx.ts#L58) 没有页面调用。回录不能填写实际赔率/金额差异；开奖表单仅全场比分，固定 `void:false`，没有半场输入。

因此“Web 全流程可用”过强。最小补齐是用队名/销售编号完成一个 had 纸面路径；未开放玩法明确限制。无需先建完六页。[E2E](../../../apps/web/e2e/smoke.spec.ts#L1) 目前只验证导航、a11y和降级，应增加真实API隔离数据的完整用户路径。

### 7. 奖池只有结构与部分判定，未形成奖金结算产品

[池票结算](../../../apps/backend/src/goalx_backend/services.py:231) 组合判定后金额为0；池票 live 未形成完整扣款/兑付，票汇总未纳入组合，14场奖级等也未覆盖。把它称为原型或待实现即可。暂不为保持“全玩法”口号补齐所有玩法。

## 数据与回测：结论须降到证据支持的范围

### 历史 sharp 基准发生质量变化

football-data 官方公告：自 **2025-07-23** 起 Pinnacle 公共API导致开盘和收盘数据系统性陈旧，已从其市场均值/最大值计算中排除。[官方数据说明](https://football-data.co.uk/data.php)

当前 [fair_probs_from_close](../../../apps/backend/src/goalx_backend/backtest.py:125) 只要 PSC 存在就优先使用，没有日期/质量判定。本库5,238条预测中1,747条在警告日期后，其中893条使用PSC，854条使用AvgC；893条是需重点复核的范围，不代表逐条已确认错误。需按日期与来源拆分、做替代基准敏感性分析，再引用“显著劣于sharp收盘”的精确结论。此建议要求重开 ADR 0007 的来源有效性约定。

### 合成价实验不是陈盘策略回测

[模拟价](../../../apps/backend/src/goalx_backend/backtest.py:136) 实际公式是 `O_sim=(1-h)/p_market`。spec/ADR把概率写成“价×haircut”，单位混淆，应明确概率、十进制赔率、折价比例。

[周内串关](../../../apps/backend/src/goalx_backend/backtest.py:404) 看整周最终收盘衍生 EV 再挑两场，没有两场共同可下单时点；单关也无真实单固资格。训练无前视不等于候选选择可执行。保留该实验作为模型相对市场及折价敏感性基线，不用它证明 A 方向有效或无效。

还没有动态资金、未结占用及纪律约束，不应把它展示为既定真钱资金曲线。当前负值支持“这份实验没有证明盈利”，不支持“陈盘机会已证伪”。

### 校准与统计的小修，优先于新模型

- [haircut](../../../apps/backend/src/goalx_backend/haircut.py:46) 独立取最新竞彩与欧赔，时间字段随后丢弃，没有最大时间差；会把市场移动与机构折价混在一起。三选择样本也不是三场独立比赛。
- [玩法汇总](../../../apps/backend/src/goalx_backend/evaluation.py:263) 每腿重复加入同一串关。主库 overall=4,154注，had=4,675注，恰好多521张2串1；应按每注唯一玩法去重。
- [显著性检验](../../../apps/backend/src/goalx_backend/evaluation.py:95) 的方差写法与标称 Newey-West 不一致，取样没有稳定时间顺序，同周/同场相关性也未充分处理。先报告差值、样本与分期结果；p值标探索性。需要时再做按周分块检验，不必堆更多统计量。
- README 的“flat ROI −25.3%”实际上接近投入加权 ROI；按每注收益率均值计算 flat ROI 约−25.19%。明确两者即可。
- [模型工件身份](../../../apps/backend/src/goalx_backend/dc_model.py:212) 只用数据指纹命名，同数据不同配置可覆盖旧文件；版本应包含参数/配置。Forecast已保存矩阵，因此不是预测立即丢失。
- bootstrap 区间不等于实证校准的预测不确定性，更不自动进入稳健Kelly；先补名义水平与覆盖证据，不应宣传“样本薄就自然安全”。

## 研究与决策：保留方向，撤下无证据的精确数字

研究已经识别模型不应单独承诺盈利、市场较强、成本重要，方向有价值。但“理论可行→国内机会已存在→明确收益/频率→实现需求”的推断跨度过大。

- **陈盘每轮1–5场、收益1–5%；滚存轮5–20%；组合1–3%。** 尚无本项目实证支持，不应用作产能/预算默认。标“待验证情景”，给来源、假设、失败条件；删数字不损害功能。
- **市场去水概率不是‘真概率’。** 它是估计基准，有源质量与模型误差；共识也不自动优于任何单源。
- **澳客人气不是已识别的真实资金份额。** 即使来源可抓，仍需证明样本与投注额/组合分布之间的关系。逐场边际人气不能唯一决定14场组合的人气，不能直接给出独中或分奖人数分布。
- **奖池EV不能只算独中。** 基本结构应是各结果/奖级下分得奖金的期望减成本；“独中概率×总池”仅覆盖其中一种情景，会遗漏共同中奖和其他奖级。先做规则/数据敏感性表，不急做覆盖优化器。
- **27%宏观返奖差额不等于每个选项固定27%劣势。** 不能无条件推出任何单注都需要相对+37%模型优势；应使用实际报价及其市场概率，分清资金返奖比例与逐注EV。

一手来源抽查补充：

- [OPAP论文作者所在大学的摘要](https://pure.unic.ac.cy/en/publications/market-efficiency-and-the-greek-fixed-odds-betting-market/)对应的题名为 *Market efficiency and the Greek fixed-odds betting market*，与研究14来源列表的Kuypers题名不符。其有限的未吸收信息发现不能推出国内每轮频率或利润率；本次读到摘要，没有声称复验论文收益结果。
- [Moffitt与Ziemba论文](https://arxiv.org/html/1801.02958v1)显式计入共同中奖者份额；它支持“需建模分奖”，不支持本项目的国内收益数字。原引用DP-103未成功提取正文，本轮不声称核实其中具体公式。
- [The Odds API官方文档](https://the-odds-api.com/liveapi/guides/v4/)按markets×regions计算普通请求用量，历史赔率请求为其10倍；按sport逐次请求时还要乘请求数量。预算可行性须基于实际采样计划。

## 成本与运行：小系统最该早算的账

按当前配置月480 credits，平均30天只有16/day；README设想40/day，持续运行约12天即耗尽月额，且常规采集与收盘采集共用预算。提高日限额不能解决月限额。优先按实际在售目标赛事与有效窗口采样，再判断是否升级数据。

[采集代码](../../../apps/backend/src/goalx_backend/ingest/oddsapi.py#L245) 只在整轮开始检查一次预算，整轮后统一记账。临近限额仍可跨越；中途失败时先前成功请求的消耗可能未记。每次请求前预估、返回后按实际用量记账即可，不需要独立配额服务。当前解析只支持h2h，不要因参数支持totals就付费请求后丢弃数据。

[持续采集票](../goalx-quant/issues/19-m1-jingcai-ingest.md) 已关闭但明确“三天连续运行待部署”。应将代码完成与运维验收分开，补运行证据，不必重做复杂项目管理体系。

成本模块应提前缩成最小台账与月度净结果。固定订阅费已经发生时，不宜任意按预计注数分摊后当作每一注的边际成本；总经济性必须仍扣固定费。分别展示：候选边际收益、增量成本、系统总成本/净结果。文档中的2% yield是情景，不应通过增加下注或本金来追求覆盖成本。

## 不必要或应推迟的投入

| 项目 | 建议 | 再启动条件 |
|---|---|---|
| 更换SQLite/FastAPI/React/Prefect | 不做 | 当前规模有具体故障证据 |
| 新一轮LLM框架比较 | 不做 | 现框架存在真实阻塞 |
| 全赛事scout、analyst、自动似然融合 | 暂缓 | 小样本前瞻显示可靠新增信息 |
| 全玩法同时达成产品级支持 | 暂缓 | 已支持玩法的数据和结算闭环稳定 |
| 奖池组合优化、复杂分配 | 暂缓 | 规则、池数据、份额误差可评估 |
| xG/GBM/stacking/新模型竞赛 | 暂缓 | 有明确失败分析、固定外样本协议 |
| 通用插件系统、消息总线、特征平台、分布式部署 | 不做 | 当前个人工具没有需求 |
| 删除已有空表以“精简架构” | 不急 | 它们产生具体维护成本时 |
| 完整设置中心和六页齐备 | 暂缓 | 固定少量参数不能支持真实用户流程 |
| 新的长期承诺文档 | 不扩张 | 修正现有事实和偏离记录已足够 |

## 建议的最小下一阶段

1. **可信度修复**：正确规则、购买幂等/资金、结果更正、验证未知状态、CLV/Forecast时间边界、统计去重。
2. **一个真实可用的纸面路径**：今日选择→锁定建议与报价→纸面/实际回录区分→开奖→结算→独立前瞻指标。用用户操作而非API独立调用验收。
3. **有限预算连续采样**：沿用Prefect，跑出每日覆盖率、同步价时差、报价过期、收盘缺失、匹配错误、credits和人工耗时。短期只判断工程与可观察性。
4. **再决策A/B及LLM**：优先A的小范围前瞻；B先核实池数据。无信号时记录“未观察到”，不要通过扩大模型和功能来绕过结论。

建议重开的是少数决策，而不是重做整份设计：验证口径及来源有效性、下一阶段主攻顺序、LLM/奖池扩展触发条件。它们记录在本轮地图的待决票中；本轮不替用户关闭HITL决策。
