# 票33实施证据

最新状态：PR #4已由用户合并至main，合并提交`4710363bedd9f417343e9047cf8c8973317b39f0`，[合并后CI](https://github.com/cosmos-arc/goalx/actions/runs/34736411426)成功。下文保留交付当时记录，未部署。

2026-09-13，分支codex/trusted-paper-plan。范围为结算与回录生命周期；其余纠偏票保持原边界。

## 原因与回归

原无效腿分支以有效腿不足2为退款条件；回录无重复/状态校验；兑付只检查live；结算批跑仅处理open，事实覆盖后不会重算已结注。

新增6个最小回归先复现错误：2元2串1剩1.8赔率有效腿，中/错均错误退款2元；未购live命中错误收入20；同请求重复ID与二次回录均重复扣款；赢改输仍为won。首次无效腿测试使用了错误的keyword-only构造方式，修正测试后重新确认业务RED，再修改实现。

实现沿用SQLite与现有服务/仓储分层，只新增两个审计表与只读核查命令。BEGIN IMMEDIATE序列化回录和结算；嵌套调用用savepoint，失败不丢调用方未提交工作。开奖更正保存原因、旧新事实及结算历史，追加差额资金事件。半场事实撤回可退回待核，补全恢复计奖。旧异常先报人工核查，不静默修复。

## 验证

最终[task check日志](ticket33-check.log)对应exit 0：179后端测试，94.17%覆盖率；19前端测试；1契约一致性测试；4项Playwright冒烟。并发回录、三种写入失败回滚、赢输双向冲正、同事实幂等、未购隔离、v3到v4保留旧账均有回归。

契约变更限于更正原因字段、业务拒绝的400/404响应说明与池票草稿说明；生成类型同步。契约门禁先因尚未暂存合法变更而失败，核对并暂存生成文件后通过；未修改门禁。

[本机旧账核查](ticket33-ledger-audit.json)使用只读连接，不迁移、不改账；返回schema v4、manual_review空、无审计记录。另行只读查询为39场Fixture、0 Bet；空账本不证明真实票据生命周期已跑通。本轮未运行运行库迁移或结算命令。

## 验收边界

本地CODE/TEST/CONTRACT通过；尚未合并或部署。浏览器冒烟只覆盖现有壳层及降级，不是票36实际had流程；真实销售日、收盘与成本验收留给票37。paper/live分层、统计分母、赛前证据与未知不通过留给票34。无新订阅、无真钱下单、无新架构平台。

交付提交：`59756ebb66cd187948246c012e7e30b3df48f7ec`，已推送至 [PR #4](https://github.com/cosmos-arc/goalx/pull/4)。本地工作区干净，忽略目录中的计划、票据与证据保留。远端CI状态另行核查，PR由用户决定合并。

远端验证：[CI run 34736084450](https://github.com/cosmos-arc/goalx/actions/runs/34736084450) 对应上述提交，CI gate及Security gate全部通过，含CodeQL、OSV、历史密钥扫描、契约、类型、覆盖率、构建与浏览器冒烟。[检查状态日志](ticket33-ci.log)由`gh pr checks 4 --watch`生成，exit 0。尚未合并或部署。
