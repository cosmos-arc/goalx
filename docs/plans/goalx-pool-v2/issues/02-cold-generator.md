# 搏冷生成器（后端+UI）

Type: impl
Status: resolved
Blocked by: 01

## 问题

页脚承诺的"搏冷模式生成器随 v2 上线"未兑现；用户选完基础票后没有工具化的冷门变体。

## 范围

1. 后端 `POST /api/v1/pool/cold-variants`：入参 period_no + 基础选择（缺省=各场最优概率）+ 冷度档位（1-3 处冷门替换）；出参变体列表：每变体=替换明细、命中概率（各场 p 连乘）、估计派彩（份额口径）、EV、三档额度（复用 staking）
2. 变体生成 v1 贪心：按"冷选项 EV − 基础选项 EV"降序替换 top-N（N=冷度）
3. contract-first：导出+codegen+conformance
4. 前端：彩池页生成器面板（选完基础票→一键生成→变体对比表→提交进纸面篮）
5. 单测：贪心选择、EV/概率计算、契约；e2e 双跑（死后端降级态+活后端）

## 不做

- 全局优化（枚举 C(14,N) 冷门组合按 EV 排序已够 14/任9 规模，但 v1 只出贪心 top 变体）

## Answer（2026-09-19）

PR #31（与 01/03 同 PR）：POST /api/v1/pool/cold-variants + 彩池页面板；贪心变体/估值/采用回填全落地；e2e 双跑绿。真实期次首验：26132 无正 EV 冷门（去水概率与份额锁步属结构性），诚实空变体。
