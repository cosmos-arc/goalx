# 07: 14场·任9 骨架页

**What to build:** 玩法组第三页骨架：期次选择 → 14/9 场列表（每场三向概率 + 推荐/搏冷两档选择标记）→ 三档额度建议（保守/标准/激进，复用 06 仓位规则）→ 提交占位。AI 证据总结、目标金额反推、搏冷模式生成器均留位标注。彩池数据未接入前以空态+示例骨架呈现（外部依赖 goalx-quant 立票）。

**Blocked by:** 03（导航组）、06（三档额度复用）

**Status:** resolved

- [x] 期次→场次概率矩阵 UI（概率来自模型；奖池口径标注"待彩池数据"）
- [x] 推荐选择标记 + 三档额度建议（复用 06）
- [x] AI 总结/目标金额/搏冷留位（not-available 态）
- [x] 空态诚实（无期次数据时说明依赖）；axe/e2e

## Answer

**路由**：`/markets/pool` 占位换真实 `MarketPoolPage`（与 MarketTabs 第三入口对齐；`MarketPlaceholderPage` 随最后一个消费者下线删除）。

**诚实原则（骨架一眼可辨）**：页头横幅级 `pool-skeleton-banner`（warning 色）声明"骨架页——彩池数据源接入中（goalx-quant 立票，外部依赖）"；期次=演示（业务日聚合场次窗口，真实期次随数据源）；14 槽位缺数据处虚线"第 N 场：待期次数据"；三向概率**占位=欧共识去水**（had 模型概率与彩池派彩口径待数据源——TodayFixture 无批量模型概率，故按票面"或'待数据'"分支落地：无共识概率的场显示"待数据"）；推荐/搏冷标记=纯前端按占位概率最高/最低向标注（页内明示"非生成器产出"）。后端不可用时降级 EmptyState（backend-unavailable + 重试），骨架结构照常渲染。

**结构与交互（完整可演示）**：
- 期次 select（演示=业务日）→ 切换清零选择；14 场槽位列表（窗口内该业务日场次，超出 14 截断）；一场一选 toggle（再点同向=取消，换向=替换）；
- 已选计数 `已选 N/14`（任9 需 ≥9）+ 复式组合数 C(N,9)（导出 `combinations` 单测 C(14,9)=2002）；"按推荐标记预选（演示）"一键填前 9 个有数据槽位的共识最高向；
- **三档额度建议（复用 06 端点，映射页内标注）**：`保守 = flat（纸面红线，mode=paper）/ 标准 = ¼Kelly 单注上限 2%（live, cap_fraction=0.02）/ 激进 = ¼Kelly 单注上限 5%（live, cap_fraction=0.05）`——StakeAdviceNote 加 `label` prop 复用；输入=所选组合联合口径（占位：共识 EV × 竞彩价，联合赔率 Π odds、联合 EV Π(1+EV_i)−1，缺价/EV 整体降级"缺 EV/赔率数据"）；EV≤0 时三档诚实 ¥0；注数=C(N,9) × ¥/注（每注同额）；真金档位标注"依赖前瞻 skill 过线，当前仅演示规则计算"；
- AI 证据总结 / 目标金额反推 / 搏冷生成器 = 三块 EmptyState not-available（"随 M3 LLM 线上线"/"随 v2 上线"）；
- 提交占位 = disabled 按钮 + 说明（复式票落库走 pool-slips 端点，待期次数据就绪后接线；本页不生成真实注单）。

**测试**：页 10 用例（横幅/口径行、期次聚合与空槽、一场一选 toggle、推荐预选封顶 9 与 C(10,9)=10 注实算、三档映射与 ¥0/¥100.08 两路径、留位三块、提交 disabled、后端不可用降级+重试）；smoke 加 pool 骨架结构断言（横幅/期次/14 槽位/三档映射/留位 3/disabled）；axe 修复横幅琥珀底 muted 对比不足（4.12<4.5 → 换前景色）；185 单测 / 85.54% branches（门 85）；paper-loop 12 步全绿；web build 过。

**commit**：`feat/wb-06-stake` 分支第二笔 `feat(web): 14场任9 骨架页…`（纯前端票零后端/contract 改动）。
