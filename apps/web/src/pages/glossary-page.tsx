import { StubPage } from "./stub-page";

/** 票 18（词典页）落地前的占位：释——指标口径与判读方向，tooltip 就地解释。 */
export function GlossaryPage() {
	return <StubPage title="词典" message="词典页收录指标口径与判读方向。" hint="随票 18（指标词典）上线。" />;
}
